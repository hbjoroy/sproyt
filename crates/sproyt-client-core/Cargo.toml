[package]
name = "sproyt-client-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false
description = "Pure browser-client transport decisions for Sproyt"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
